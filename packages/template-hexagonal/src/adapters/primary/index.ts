/**
 * Primary (Driving) Adapters
 *
 * These adapters are called by external systems (API, CLI, UI).
 * They implement the input ports and call the application layer.
 */

// Re-export adapters for use by consuming applications
// Example adapters:
// - RestApiAdapter: HTTP REST API endpoints
// - CliAdapter: Command-line interface
// - GraphQLAdapter: GraphQL resolvers
