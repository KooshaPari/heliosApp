name: Monthly SBOM Refresh
on:
  schedule:
    - cron: '0 0 1 * *'  # 1st of each month
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-sbom-refresh:
    uses: KooshaPari/phenotype-tooling/.github/workflows/sbom-monthly.yml@c19fe8dbde0685626da9cc5da3a6923412dbd5a3
    with:
      repo_path: '.'
      build_system: 'node'
