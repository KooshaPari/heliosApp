name: Security Guard (Hooks)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "**"

permissions:
  contents: read

jobs:
  guard:
    uses: KooshaPari/phenoShared/.github/workflows/security-guard-hook-audit.yml@d95bb6755390f73c56ea496b246d098fbf97bbe9
